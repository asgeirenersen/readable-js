export const isTrue = val => val === true;
export const isNotTrue = val => val !== true;

export const isFalse = val => val === false; 
export const isNotFalse = val => val !== false;

export const trueIfNotFalse = val => val === false;
export const falseIfNotTrue = val => val === true;
